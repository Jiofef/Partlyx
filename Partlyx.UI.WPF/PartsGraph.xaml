<UserControl x:Class="Partlyx.UI.WPF.PartsGraph"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:graphvm="clr-namespace:Partlyx.ViewModels.Graph;assembly=Partlyx.ViewModels"
             xmlns:local="clr-namespace:Partlyx.UI.WPF.OtherControls"
             xmlns:zoom="https://github.com/Moravuscz/WPFZoomPanel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource PxWhiteBrush}">
    <zoom:ZoomPanelScrollViewer ScrollZoomModifier="" ZoomAndPanInitialPosition="FitScreen" PanningMode="Both" Background="Transparent" ViewportZoom="0.9" BorderBrush="Transparent">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <!-- edges behind -->
            <ItemsControl ItemsSource="{Binding Edges}" Height="2000" Width="2000">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:FromToLineControl From="{Binding From}" To="{Binding To}" Brush="{StaticResource PxBlackBrush}" LineThickness="2"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <!-- nodes on top -->
            <ItemsControl ItemsSource="{Binding Nodes}">
                <ItemsControl.Resources>
                    <DataTemplate DataType="{x:Type graphvm:RecipeGraphNodeViewModel}">
                        <Border Width="{Binding Width}" Height="{Binding Height}" CornerRadius="4" Padding="2" Background="{StaticResource PxWhiteBrush}"
                            BorderBrush="{StaticResource PxGrayBrush}" BorderThickness="3">
                            <StackPanel VerticalAlignment="Center" Orientation="Vertical">
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Value.UiNode.ColumnText}"/>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Value.UiNode.SecondaryColumnText}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type graphvm:ComponentGraphNodeViewModel}">
                        <Border Width="{Binding Width}" Height="{Binding Height}" CornerRadius="4" Padding="2" Background="{StaticResource PxWhiteBrush}"
                            BorderBrush="{StaticResource PxGrayBrush}" BorderThickness="3">
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Value.UiNode.ColumnText}"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.Resources>
                
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top"  Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Grid>
    </zoom:ZoomPanelScrollViewer>
</UserControl>
